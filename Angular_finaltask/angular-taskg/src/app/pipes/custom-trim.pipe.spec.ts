import { CustomTrimPipe } from './custom-trim.pipe';

describe('CustomTrimPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomTrimPipe();
    expect(pipe).toBeTruthy();
  });
});
